import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { DetailhistoryPage } from './detailhistory';

@NgModule({
  declarations: [
    DetailhistoryPage,
  ],
  imports: [
    IonicPageModule.forChild(DetailhistoryPage),
  ],
})
export class DetailhistoryPageModule {}
